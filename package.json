{
  "name": "ct-samples",
  "version": "1.0.0",
  "description": "A small project for hosting samples locally. Spins up a local server and creates a .json file of contents'",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node toJSON.js && serve samples/"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "directory-tree": "^3.3.1",
    "fs": "^0.0.1-security",
    "serve": "^14.0.1"
  }
}
